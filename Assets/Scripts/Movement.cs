using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Movement : MonoBehaviour
{
    /*
    public enum MovementFunction
    {
        Nonlinear,
        LinearDirect
    }

    public MovementFunction movement = MovementFunction.LinearDirect;
    public float additionalMovementMultipler = 1.0f;
    public float headZoneRadius = 0.25f;

    private Vector3 relativeMovementOfCameraRig = new Vector3();
    private Transform movementTransform;
    private Vector3 headCirclePosition;
    private Vector3 lastPosition;
    private Vector3 lastPosition;
    private Vector3 lastMovement;
    private bool activationEnabled;
   
    private virtual void OnEnable()
    {
        activationEnabled = false;
        movementTransform = 

    }
    */

}
